export const globals = {
    // number of days from now allowed for reservation
    NUM_DAYS: 7,
    // number of time slots in total everyday
    NUM_SLOTS: 10,
    // number of tables
    NUM_TABLES: 10,
};